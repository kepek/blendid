{
  "name": "bayzat-assets-THEMENAME",
  "version": "1.0.0",
  "main": "echo \"Error: no main specified\" && exit 1",
  "scripts": {
    "build": "echo \"Error: no build script specified\" && exit 0",
    "clean": "echo \"Error: no clean script specified\" && exit 0",
    "create": "echo \"Error: no create script specified\" && exit 0",
    "delete": "echo \"Error: no delete script specified\" && exit 0",
    "install": "echo \"Error: no install script specified\" && exit 0",
    "lint": "echo \"Error: no lint script specified\" && exit 0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "bayzat",
    "bayzat-assets"
  ],
  "license": "MIT",
  "devDependencies": {
    "blendid": "github:kepek/blendid#44.0.1"
  }
}
